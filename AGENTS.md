# AGENTS.md

## Go Toolchain Requirement

- Before doing any substantial work in this repository, check whether `go` is installed and available on `PATH`.
- If `go` is not installed, stop immediately and ask the user to install Go before proceeding.
- Do not attempt implementation, formatting, or test execution until the user has installed Go and it is accessible as `go`.
